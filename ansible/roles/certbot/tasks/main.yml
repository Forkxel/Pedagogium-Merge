# Disabled in site.yml — TLS certs are provisioned manually for dev.
# Intended for production Let's Encrypt certificate issuance.

- name: Install Certbot with Nginx plugin
  apt:
    name:
      - certbot
      - python3-certbot-nginx
    state: present
    update_cache: yes

- name: Obtain certificate for domain
  command: >
    certbot --nginx
    --non-interactive
    --agree-tos
    --email {{ cert_email }}
    -d {{ domain_name }}
  args:
    creates: /etc/letsencrypt/live/{{ domain_name }}/fullchain.pem  # Idempotent — skips if cert exists